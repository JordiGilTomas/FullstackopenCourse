title exercise 0.6

browser->server: HTTP POST https://fullstack-exampleapp.herokuapp.com/new_note_spa
server->browser: {"message":"note created"}

note over browser:
When submit the form a HTTP POST is done and the server
returns a json whit the message "note created"
without reloading the page.
end note